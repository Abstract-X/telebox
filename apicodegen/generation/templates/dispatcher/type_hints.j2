{{ imports }}


Event = Union[
    {% for i in hint_types %}
    {{ i }}{% if not loop.last %},{% endif +%}
    {% endfor %}
]
Handler = Callable[["EventContext"], None]
ErrorHandler = Callable[["EventContext"], None]
